# 图片课表识别提示词

请仔细识别我随后发送的课程表图片，并把内容转换为下方规定的 JSON。该 JSON 将直接导入“课程表排版工坊”，因此必须严格遵守结构。

识别规则：

1. 图片中的列从左到右对应星期一、星期二、星期三、星期四、星期五。
2. 逐行识别科目与时间，不得根据常识补写看不清的内容。
3. 无法确认或空白的科目写成空字符串 `""`，并在 `notes` 中标注对应位置和原因。
4. 每个普通课程行的 `subjects` 必须恰好包含 5 个字符串，顺序固定为星期一至星期五。
5. 课间操、眼保健操、午休、晚饭等合并行使用 `"break": true`，不能添加 `subjects`。
6. 午休行另外写 `"noon": true`。
7. 时间统一写成 `H:MM-H:MM`；只规范连接符，不擅自修改图片中的时间。
8. 节次名称固定使用：早读、一、二、课间操、三、眼保健操、四、午   休、五、六、七、八、九、晚饭、晚读、自习。
9. `rows` 必须恰好包含以下 16 行且保持顺序；图片缺失的整行仍保留，未知内容写空字符串。
10. 只输出一个 JSON 代码块，不要在代码块前后解释，也不要使用 Markdown 表格。

严格按此格式输出：

```json
{
  "format": "timetable-workshop",
  "version": 1,
  "updatedAt": "填写识别时的 ISO 8601 时间；不知道则留空字符串",
  "rows": [
    { "label": "早读", "time": "7:00-7:30", "subjects": ["", "", "", "", ""] },
    { "label": "一", "time": "", "subjects": ["", "", "", "", ""] },
    { "label": "二", "time": "", "subjects": ["", "", "", "", ""] },
    { "label": "课间操", "time": "", "break": true },
    { "label": "三", "time": "", "subjects": ["", "", "", "", ""] },
    { "label": "眼保健操", "time": "", "break": true },
    { "label": "四", "time": "", "subjects": ["", "", "", "", ""] },
    { "label": "午   休", "time": "", "break": true, "noon": true },
    { "label": "五", "time": "", "subjects": ["", "", "", "", ""] },
    { "label": "六", "time": "", "subjects": ["", "", "", "", ""] },
    { "label": "七", "time": "", "subjects": ["", "", "", "", ""] },
    { "label": "八", "time": "", "subjects": ["", "", "", "", ""] },
    { "label": "九", "time": "", "subjects": ["", "", "", "", ""] },
    { "label": "晚饭", "time": "", "break": true },
    { "label": "晚读", "time": "", "subjects": ["", "", "", "", ""] },
    { "label": "自习", "time": "", "subjects": ["", "", "", "", ""] }
  ],
  "notes": [
    "如：星期三第七节字迹模糊，已填写为空字符串"
  ]
}
```

输出前自行检查：JSON 语法有效；所有键名使用双引号；没有注释或尾随逗号；普通行均为 5 个科目；识别结果与图片逐格对应。
